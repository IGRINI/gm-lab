<% if detailed %><% if names %>Keep these proper nouns exactly as written if they appear; never translate or transliterate them: << names >>.<% else %>Keep proper nouns exactly as written in the transcript; never transliterate them.<% endif %><% else %><% if names %>Keep these proper nouns exactly as written: << names >>.<% else %>Keep proper nouns exactly as written; never translate or transliterate them.<% endif %><% endif %>
